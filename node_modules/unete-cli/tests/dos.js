const UneteIO = require('unete-io/socket');
const Server = UneteIO('http://localhost:5555');

(async () => {
    for(let i=0;i<1000000;i++) {
        process.stdout.write(`\n${i}`);
        await Server.step(() => "");
    }
})();